const Plugin = require('./src/util/plugin');
const UI = require('./src/ui');
const PomodoroJS = require('./src/main');
const EVENTS = require('./src/core/events');

module.exports = {
  EVENTS,
  Plugin,
  UI,
  PomodoroJS,
};
